const APIError = require('./../api-error');

class Authentication {

    /**
     * This is where the authentication logic of the API should be stored.
     */
    static async authenticationLogic(req, res, next) {
        
    }

}

module.exports = Authentication;